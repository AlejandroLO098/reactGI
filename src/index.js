import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <div>
    <App />
  </div>
);

// VERY EASY: Inside of your ‘App’ class ‘render’ method, return a div with your basic
// information for example name, number, date of birth and etc. This div should be hard-coded
//  to prepare for the Medium Challenge.
